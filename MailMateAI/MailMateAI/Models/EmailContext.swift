import Foundation

/// Type alias so the host app can reference the shared model conveniently.
/// The canonical definition lives in Shared/EmailContextModel.swift.
typealias EmailContext = EmailContextModel
